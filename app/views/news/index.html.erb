<div id="news">
  <%= link_to 'お知らせ', '#' %>
  <% @news.each do |news| %>
    <div>
      <div><%= news.title %></div>
      <div><%= news.published_at.strftime('%Y年%m月%d日') %></div>
  <% end %>
</div>
